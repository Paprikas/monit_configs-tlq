#Monitoring redis
check process redis with pidfile <%= node['monit_configs-tlq']['redis']['pidfile'] %>
  group database
  start program = "<%= node['monit_configs-tlq']['redis']['initfile'] %> start"
  stop program = "<%= node['monit_configs-tlq']['redis']['initfile'] %> stop"
  if failed host 127.0.0.1 port 6379 then restart
  if 15 restarts within 15 cycles then timeout
